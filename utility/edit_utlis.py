from utility.display_utils import format_note, format_user


# WIP


def edit_note(note):
    while True:
        format_note(note)
        edit_attribute()

def edit_user(user):
    format_user(user)